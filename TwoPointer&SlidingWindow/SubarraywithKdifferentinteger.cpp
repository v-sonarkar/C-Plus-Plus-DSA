// Subarray with K Different Integers
// Given an array of integers nums and an integer k, return the number of subarrays with exactly k different integers.
// Time Complexity: O(n) where n is the size of the input array

#include <iostream>
#include <vector>
#include <unordered_map>
using namespace std;

int subarraysWithKDistinct(vector<int>& nums, int k) {

      
    

    












}

int main() {
    vector<int> nums = {1, 2, 1, 2, 3};
    int k = 2;

    cout << "Number of subarrays with exactly " << k << " distinct integers: " << subarraysWithKDistinct(nums, k) << endl; // Expected output: 7

    return 0;
}
